// depth first search
// https://en.wikipedia.org/wiki/Depth-first_search
import GraphInterface from "./graph/interfaces/graph";

const dfs = (graph: GraphInterface) => {
    return graph;
}

export {
    dfs
};